# Native (Expo) env — copy to .env and adjust.
# Expo inlines EXPO_PUBLIC_* vars into the JS bundle at build time:
# they are PUBLIC config, never secrets. Declare each var in
# packages/env/src/native.ts (without the prefix).

# EXPO_PUBLIC_RELAY_URL=https://example.com
